export enum HTTP_METHOD {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum ItemType {
  FILE = "file",
  FOLDER = "folder",
}

export enum FILE_STATUS {
  CONFLICT = "CONFLICT",
  CREATED = "CREATED",
}
